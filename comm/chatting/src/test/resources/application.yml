spring:
  datasource:
    url: jdbc:mysql://localhost:3306/comm
    username: root
    password: 0405
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_batch_fetch_size: 100

logging:
  level:
    root: INFO
  file:
    name: /spring-log/application.log